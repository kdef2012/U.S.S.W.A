export const metadata = {
  title: "Tournaments | U.S.S.W.A",
  description: "Browse all upcoming United Scholastic Style Wrestling Association tournaments.",
};

export default function EventsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
